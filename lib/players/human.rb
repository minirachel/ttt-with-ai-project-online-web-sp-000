module Players
    class Human < Player

        def move(board)
            input = gets.strip
            input
            #move via CLI
            #return the value the user enters
            #does not need to use board
        end
    end
end

